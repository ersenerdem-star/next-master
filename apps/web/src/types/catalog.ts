export type CatalogRow = {
  total_count: number;
  product_id: string;
  product_code: string;
  brand: string;
  description: string;
  oem_no: string;
  hs_code: string;
  origin: string;
  weight_kg: number | null;
};
