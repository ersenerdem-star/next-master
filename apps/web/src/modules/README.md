# App Modules

This folder is the owned feature boundary for the frontend.

## Ownership

- `admin/` for internal operations.
- `portal/` for customer/vendor scoped flows.
- `warehouse/` for stock execution.
- `catalog/` for the product core.
- `shared/` for cross-surface primitives only.

## Rule

Feature logic belongs here, not in `presentation/pages`.
